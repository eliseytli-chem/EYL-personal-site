---
# 文章標題。請填寫完整、清楚的中文標題。
title:

# 發布日期。使用 YYYY-MM-DD 格式，例如 2026-06-19。
date:

# 主要分類。每篇文章只填一個分類，並沿用網站既有分類名稱。
category:

# 文章標籤。使用 YAML 陣列，可填多個；例如：
# tags:
#   - 閱讀
#   - 親子
tags:

# 文章摘要。建議用一至兩句話說明內容，供列表與搜尋結果顯示。
description:

# 可見性：
# public   — 正式公開，會出現在首頁、列表、分類、標籤與搜尋。
# unlisted — 不出現在列表與搜尋，但知道網址即可閱讀。
# private  — 私人保存，不會建置成公開頁面。
# draft    — 未完成草稿，不會建置成公開頁面。
visibility: draft

# 是否列入精選文章。只接受 true 或 false；非 public 文章不會因設為 true 而公開。
featured: false
---

# 正文從這裡開始
